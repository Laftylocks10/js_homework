let reverse = (str) => {
    var str_array = [];
    for (let i = str.length - 1; i >= 0; i--) {
        str_array.push(str[i]); 
    }
    return str_array.join('');
}

let myName = "Luther";
console.log(reverse(myName)) # "rethuL"
console.log(reverse("woah")) # "hoaw"
